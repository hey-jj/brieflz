# Changelog

## [0.2.0] - 2026-07-07

### Changed
- `depack_safe` now returns `MalformedInput` when a gamma-coded match length overflows. This replaces a debug-build panic and an optimized-build short success for that malformed stream (#12).

## [0.2.0] - 2026-07-07

### Changed
- `depack_safe` now returns `MalformedInput` when a gamma-coded match length overflows. This replaces a debug-build panic and an optimized-build short success for that malformed stream (#12).
